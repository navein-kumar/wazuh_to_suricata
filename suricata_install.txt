suricata install:

sudo docker run -d --net=host --restart always  --cap-add=net_admin --cap-add=net_raw --cap-add=sys_nice -v $(pwd)/logs:/var/log/suricata jasonish/suricata:latest -i eth0
    
  
	
--
  <localfile>
    <log_format>syslog</log_format>
    <location>/suricata/logs/eve.json</location>
  </localfile>
